<%= form_with(model: articulo, class: "form-container") do |form| %>
  <% if articulo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(articulo.errors.count, "error") %> prohibited this articulo from being saved:</h2>

      <ul>
        <% articulo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :titulo, class: "label-form" %>
    <%= form.text_field :titulo , class: "input-form"%>
  </div>

  <div>
    <%= form.label :contenido, class: "label-form" %>
    <%= form.text_area :contenido, class: "input-form" %>
  </div>

  <div>
    <%= form.label :usuario_id, class: "label-form" %>
    <%= form.text_field :usuario_id, class: "input-form" %>
  </div>

  <div>
    <%= form.submit class: "submit-form" %>
  </div>
<% end %>
