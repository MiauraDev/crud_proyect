<%= form_with(model: comentario, class: "form-container") do |form| %>
  <% if comentario.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(comentario.errors.count, "error") %> prohibited this comentario from being saved:</h2>

      <ul>
        <% comentario.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :contenido, class: "label-form" %>
    <%= form.text_area :contenido, class: "input-form" %>
  </div>

  <div>
    <%= form.label :usuario_id, class: "label-form" %>
    <%= form.text_field :usuario_id, class: "input-form" %>
  </div>

  <div>
    <%= form.label :articulo_id, class: "label-form" %>
    <%= form.text_field :articulo_id, class: "input-form" %>
  </div>

  <div>
    <%= form.submit class: "submit-form" %>
  </div>
<% end %>
